# Smart Food Ordering — Frontend environment examples
# Copy to `.env.local` (or `.env`) and adjust for your backend.

# Base URL of the FastAPI backend.
# The frontend API client reads this value at build/runtime.
# Example (local development):
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000