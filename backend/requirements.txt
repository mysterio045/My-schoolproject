# Smart Food Ordering Backend - Dependencies
# Python 3.12+

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database (Async PostgreSQL) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# --- Configuration ---
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt must be pinned to <4.1 — passlib 1.7.4 is incompatible with bcrypt>=4.1
# (raises 'module bcrypt has no attribute __about__').
bcrypt==4.0.1

# --- Validation (Pydantic v2 EmailStr) ---
email-validator==2.2.0

# --- Utilities ---
python-multipart==0.0.20

# --- Testing ---
httpx==0.27.2
